// Write a program asking user to enter two numbers, x and n, then check if x is in lower
// half or higher half of n

// let x = Number(prompt(`enter x number`));
// let n = Number(prompt(`enter n number`));
// if (x<n/2) {
//     console.log(`lower half of n`);
// } else console.log(`${x} higher half of ${n}`);