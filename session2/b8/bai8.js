// Write a script to check if a number is even (divisible by 2) or odd number

// let x = Number(prompt(`nhap so can check`));
// if(x%2 == 0) {
// alert(`${x} la so chan`);
// } else alert(`${x} la so le`);