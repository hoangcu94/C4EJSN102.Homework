// A square

    // for (i=1; i<=4; i++) {
    // fd(100)
    // rt(90)
    // }

// An equilateral triangle

    // rt(30)
    // for(i=1; i<=3; i++) {
    // fd(100)
    // rt(120)

// A circle

    // let x = prompt(`nhap x`);
    // for (let i=1; i<=x; i++) {
    // fd(1)
    // rt(360/x)
    // }

// Multi-circle

    // let a = prompt(`nhap a`);
    // let b = prompt(`nhap b`)
    // for (let y=1; y<=b; y++) {
    // for (let i=1; i<=a; i++) {
    // fd(1)
    // rt(360/a)
    // }
    // rt(360/b)
    // }
